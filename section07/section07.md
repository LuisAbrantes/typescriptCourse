# The All-Important Interface

Interfaces => Creates a new type, describing the property names and value types of an object, like number, string, etc.

## General Strategy for Resusable Code in Typescript

1. Create functions that accept arguments that are typed with interfaces.
2. Object/classes can decide to 'implement' a given interface to work with a function.
